安装neovim
```
brew install neovim
```

删除nvim
```
rm -rf ~/.local/share/nvim
rm -rf ~/.config/nvim/
```

安装macism
```
brew tap laishulu/homebrew
brew install macism
macism
```

把 nvim 目录 放到 ~/.config/nvim
一共装了两个插件
+ lazy.nvim
+ keaising/im-select.nvim
